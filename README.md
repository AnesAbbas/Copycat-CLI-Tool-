# Copycat-CLI-Tool-

A windows CLI tool for copying files quickly

1. Clone repo.
2. Copy desired files into `files-to-copy` directory.
2. Add repo location to PATH.
3. In a CLI, in a desired location, type `copycat` or `cc` to execute and copy files to target.

![Copycat](media/Copycat.png)